// Compatibility export for older imports. The active browser Firebase configuration
// lives in lib/firebase.ts and is populated exclusively through NEXT_PUBLIC_FIREBASE_*.
export { default, auth, db } from './lib/firebase';
